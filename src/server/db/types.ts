export enum Role {
    APPLICANT = "applicant",
    MEMBER = "member",
    ADMIN = "admin"
}

export enum FieldType {
    BOOLEAN = "boolean",
    MULTIPLE_CHOICE = "multiple_choice",
    CHECKBOX = "checkbox",
    STRING = "string"
}

